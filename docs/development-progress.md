# 实现状态

| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| C01 | 完成 | Java 17 多模块构建、Wrapper、依赖边界 |
| C02 | 完成 | 规范 CBOR、Open JSON、有界帧、独立编码向量 |
| C03 | 完成 | File/Generator 输入源、单块窗口、字节许可、摘要与 Merkle frontier |
| C04 | 完成 | HPKE Auth、mTLS 节点与 SPKI 绑定、路由和密钥授权、HTTP 资源限制 |
| C05 | 完成 | 持久输出、原子状态、force 后 receipt、有限索引、恢复与进程锁 |
| C06 | 完成 | 固定 NONE 认证传输闭环、重放与错误映射、终态 gate、输出重读验证 |
| C07 | 完成 | 可选有界 ZSTD、协商与峰值交叉校验、固定压缩材料、双进程启动示例 |
| F01 | 完成 | 恢复 receipt 发布前补齐持久性屏障；刷盘失败冻结；真实进程退出回归 |
| C08 | 待实现 | 单任务内的有界并发、范围预留与全生命周期许可 |

## 验证范围

`mvnw clean verify` 执行编译与快速测试；Windows 使用 `mvnw.cmd`。全工程包含 235 项测试，其中 17 项压缩测试、28 项真实 TLS 传输集成测试，并覆盖配置启动的双 JVM 传输。

- V01–V03：规范编码、长度与配额、独立 Merkle 根、api/core 依赖边界。
- V04–V05：标准 HPKE Auth 向量、AAD/frame 篡改、实际 TLS 身份/SPKI、路由与密钥校验。
- V06：持久 Open、相同请求重放、冲突和过期拒绝。
- V07–V09：短写、force 后 receipt、存储边界异常、残片截断、完整 CRC 损坏、载荷缺失、根目录跨进程锁。另覆盖六个真实进程退出边界、恢复刷盘失败及路径逃逸拒绝。
- V10：空流、1 字节、未知长度和跨块传输，包含超过 8 MiB 的持久输出逐字节比对。
- V11：NONE/ZSTD 的高重复、高熵及空流；帧长度、尾随数据、拼接帧、窗口和输出上限、预算不足；原生调用前后的中断检查。
- V12：Finish 幂等、缺块后补交、终态写入限制；修改、截短、扩展输出或错误 Merkle 根均不能完成。
- HTTP 边界：控制 JSON 严格投影、请求长度与编码限制、提前拒绝非法帧前缀、非阻塞 BUSY 响应、源关闭和字节许可释放。

固定闭环已补齐 NONE/ZSTD、双 JVM 启动、持久输出重读和存储进程退出证据。原生调用中途不保证可立即中断，机器断电持久性不属于进程退出测试的结论。并发调度、自动对账重试、完整生命周期恢复与性能实验未完成，不将这些能力或研究收益标记为已交付。
